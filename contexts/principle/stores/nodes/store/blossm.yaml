name: nodes
context: principal

schema:
  scene: String
  network: String

testing:
  examples:
    - put:
        scene: some-scene
        network: some.network
      get:
        scene: some-scene
        network: some.network
    - put:
        scene: some-other-scene
        network: some.other-network
      get:
        scene: some-other-scene
        network: some.other-network

#don't change
procedure: view-store
