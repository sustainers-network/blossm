name: nodes
context: principal

schema:
  scene: String
  network: String

testing:
  examples:
    first:
      put:
        scene: some-scene
        network: some.network
      get:
        scene: some-scene
        network: some.network
    second:
      put:
        scene: some-other-scene
        network: some.other-network
      get:
        scene: some-other-scene
        network: some.other-network

#don't change
procedure: view-store
