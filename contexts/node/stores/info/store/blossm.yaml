name: info
context: node
one: true

schema:
  network: String

devDependencies:
  "localenv": "^0.2.2"
  "mocha": "^7.1.0"
  "chai": "^4.2.0"
  "sinon-chai": "^3.3.0"
  "@blossm/request": "*"
  "@blossm/uuid": "*"

testing:
  examples:
    query:
      root: some-key
      first:
        put:
          network: some.network
        get:
          network: some.network
      second:
        put:
          network: some.other-network
        get:
          network: some.other-network
    index:
      put:
        network: some.domain
      query: {}
      get:
        network: some.domain
    stream:
      first:
        put:
          network: some.network
        get:
          network: some.network
      second:
        put:
          network: some.other-domain
        get:
          network: some.other-domain

#don't change
procedure: view-store
