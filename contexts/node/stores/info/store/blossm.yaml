name: info
context: node
one: true

schema:
  network: String

testing:
  examples:
    - put:
        network: some.network
      get:
        network: some.network
    - put:
        network: some.other-network
      get:
        network: some.other-network

#don't change
procedure: view-store
