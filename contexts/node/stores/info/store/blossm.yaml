name: info
context: node
one: true

schema:
  network: String

testing:
  examples:
    first:
      put:
        network: some.network
      get:
        network: some.network
    second:
      put:
        network: some.other-network
      get:
        network: some.other-network

#don't change
procedure: view-store
