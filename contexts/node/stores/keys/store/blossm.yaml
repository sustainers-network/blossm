name: keys
context: node

schema:
  name: String
  network: String

testing:
  examples:
    first:
      put:
        name: some name
        network: some.network
      get:
        name: some name
        network: some.network
    second:
      put:
        name: some other name
        network: some.other-network
      get:
        name: some other name
        network: some.other-network

#don't change
procedure: view-store
