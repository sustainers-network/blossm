name: keys
context: node

event:
  action: create
  domain: key
  service: system

devDependencies:
  "localenv": "^0.2.2"
  "mocha": "^7.1.0"
  "chai": "^4.2.0"
  "sinon": "^7.3.2"
  "sinon-chai": "^3.3.0"
  "chai-datetime": "^1.5.0"
  "@blossm/request": "*"

testing:
  examples:
    - root: some-key-root
      context:
        node:
          root: some-node-root
          service: some-node-service
          network: some-node-network
      payload:
        network: some.network
        name: some name
      result:
        query: {}
        value:
          root: some-key-root
          name: some name
          network: some.network

#don't change
procedure: projection
