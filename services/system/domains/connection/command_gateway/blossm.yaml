domain: connection
service: system

commands:
  - name: open
    basic: true
    context: node
    privileges:
      - create

whitelist:
  - "127.0.0.1"

devDependencies:
  "mocha": "^7.1.0"
  "chai": "^4.2.0"

#don't change
procedure: command-gateway
