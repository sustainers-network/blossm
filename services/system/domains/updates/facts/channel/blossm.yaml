name: channel
domain: updates
service: system

dependencies:
  "@blossm/request": "*"

testing:
  dependencies:
    - procedure: http
      host: v.some-context.some-network
      mocks:
        - method: get
          path: /
          code: 200
          response: some-channel
    - procedure: http
      host: v.some-domain.some-service.some-context.some-network
      mocks:
        - method: get
          path: /
          code: 200
          response: some-channel

  steps:
    - query:
        name: some-name
        context: some-context
        network: some-network
      context:
        some: context
      code: 200
    - query:
        name: some-name
        domain: some-domain
        some-domain:
          service: some-service
        context: some-context
        network: some-network
      context:
        some: context
      code: 200

#don't change
procedure: fact
