domain: updates
service: system

commands:
  - name: create-token
    privileges: none
  - name: push
    # context: node
    # TODO revisit
    privileges:
      none
      # - create

whitelist:
  - "127.0.0.1"

#don't change
procedure: command-gateway
