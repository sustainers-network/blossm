domain: updates
service: system

commands:
  - name: subscribe-to
    context: node
    privileges:
      - create

whitelist:
  - "127.0.0.1"

devDependencies:
  "mocha": "^7.1.1"
  "chai": "^4.2.0"

#don't change
procedure: command-gateway
