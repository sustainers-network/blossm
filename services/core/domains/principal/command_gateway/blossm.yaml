domain: principal
service: core

commands:
  # TODO it shouldn't be anyones right to add a role or scene to a principal
  - name: add-roles
    privileges: none
  - name: add-scenes
    privileges: none

whitelist:
  - "127.0.0.1"

#don't change
procedure: command-gateway
