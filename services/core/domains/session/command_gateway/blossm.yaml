domain: session
service: core

commands:
  - name: start
    protection: context
  - name: save
    privileges: none
  - name: terminate
    privileges:
      - update
  - name: change-scene
    privileges:
      - update

whitelist:
  - "127.0.0.1"

#don't change
procedure: command-gateway
