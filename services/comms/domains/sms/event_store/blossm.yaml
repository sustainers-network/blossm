domain: sms
service: comms

schema:
  to: String
  message: String

testing:
  examples:
    first:
      action: send
      payload:
        to: some-to
        message: some-message
    second:
      action: send
      payload:
        to: some-to
        message: some-message
    more: []

  handlers:
    - action: send
      examples:
        - state:
            to: some-to
            message: some-message
          payload:
            to: some-other-to
            message: some-other-message
          result:
            to: some-other-to
            message: some-other-message

#don't change
procedure: event-store
